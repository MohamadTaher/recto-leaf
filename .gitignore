# Build files
.gradle
.kotlin
build

# IDE files
*.iml
.idea/*
!.idea/icon.svg
/captures

# Configuration files
keystore.properties
local.properties

# macOS specific files
.DS_Store

# [recto-leaf] Design notes and build log — kept local, deliberately not published.
# The fork is a public repo; these are working documents, not deliverables.
# The // [recto-leaf] comments in the code reference plans/ by number.
/plans/
/progress/
